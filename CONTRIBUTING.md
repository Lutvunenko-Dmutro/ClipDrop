# Як долучитись до розробки (Contributing)

Ми дуже раді будь-якій допомозі в розвитку **ClipDrop Bot**! Якщо ви хочете додати нову фічу, виправити баг або покращити документацію, дотримуйтесь цієї простої інструкції:

## 🍴 Крок 1: Зробіть Fork репозиторію

1. Натисніть кнопку **"Fork"** у правому верхньому куті сторінки репозиторію. Це створить копію проєкту у вашому GitHub-акаунті.
2. Клонуйте ваш форк локально на комп'ютер:
   ```bash
   git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/my-telegram-bot.git
   ```
3. Перейдіть до теки проєкту:
   ```bash
   cd my-telegram-bot
   ```

## 🌱 Крок 2: Створіть нову гілку (Branch)

Завжди робіть зміни у новій гілці. Називайте її коротко і зрозуміло (наприклад, `feature/instagram-support` або `bugfix/webhook-timeout`).
```bash
git checkout -b feature/назва-вашої-фічі
```

## 💻 Крок 3: Внесіть зміни та перевірте локально

- Напишіть свій код (додайте нову платформу, оптимізуйте завантаження тощо).
- Переконайтеся, що ваш код відповідає існуючому стилю програмування проєкту.
- Запустіть бота локально, щоб перевірити, чи все працює (інструкція з запуску є в `README.md`).

## 📤 Крок 4: Закомітьте та відправте зміни

Коли ви будете задоволені результатом:

1. Додайте змінені файли:
   ```bash
   git add .
   ```
2. Зробіть коміт із чітким описом того, що ви змінили:
   ```bash
   git commit -m "Feat: Додано підтримку завантаження з Instagram Reels"
   ```
3. Відправте гілку у ваш форк на GitHub:
   ```bash
   git push origin feature/назва-вашої-фічі
   ```

## 🚀 Крок 5: Створіть Pull Request (PR)

1. Зайдіть на сторінку вашого форку на GitHub.
2. Ви побачите зелену кнопку **"Compare & pull request"**. Натисніть її.
3. Опишіть ваші зміни: що саме ви додали, чому це важливо і як це перевірити.
4. Натисніть **"Create pull request"**.

Ми перевіримо ваш код і, якщо все чудово, приймемо його до основної гілки проєкту! 🎉 Дякуємо за ваш внесок!
